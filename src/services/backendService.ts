/** * Backend API Service * Handles all communication with the FastAPI backend */const API_BASE_URL = import.meta.env.VITE_BACKEND_API_URL || 'http://localhost:8000';const API_PREFIX = '/api';export interface AnalysisResult {  score: number;  statusLabel: string;  recommendation: 'Interview' | 'Consider' | 'Reject';  confidenceScore: number;  scores: {    overall: number;    skills: number;    experience: number;    education: number;  };  competencyAnalysis: {    category: string;    candidateScore: number;    requiredScore: number;    gap: string;  }[];  skillBreakdown: {    skill: string;    status: 'MATCH' | 'CLOSE' | 'GAP';    candidateLevel: number;    requiredLevel: number;  }[];  matchingSkills: string[];  criticalGaps: {    skill: string;    classification: 'Critical' | 'Secondary';    detail: string;  }[];  strengths: string[];  weaknesses: string[];  actionPlan: {    skill: string;    timeEstimate: string;    difficulty: 'Easy' | 'Medium' | 'Hard';    impact: 'Low' | 'Medium' | 'High';  }[];  explanation: string;  radarData: { subject: string; A: number; B: number; fullMark: number }[];}export interface CandidateData {  candidateId: string;  name: string;  resumeText: string;}export interface CandidateScore extends AnalysisResult {  candidateId: string;  name: string;}export interface RankingResult {  totalCandidates: number;  rankedCandidates: CandidateScore[];}/** * Analyze resume against job description using backend API */export async function analyzeResumeWithBackend(  resumeText: string,  jobDescription: string): Promise<AnalysisResult> {  try {    const url = `${API_BASE_URL}${API_PREFIX}/seeker/analyze`;    console.log('≡ƒöù Calling backend API:', url);    console.log('≡ƒôí API_BASE_URL:', API_BASE_URL);    console.log('≡ƒöº Environment variable:', import.meta.env.VITE_BACKEND_API_URL);        const response = await fetch(url, {      method: 'POST',      headers: {        'Content-Type': 'application/json',      },      body: JSON.stringify({        resume_text: resumeText,        job_description: jobDescription,      }),    });    if (!response.ok) {      const errorData = await response.json().catch(() => ({}));      throw new Error(        errorData.error?.message || `Backend API error: ${response.status}`      );    }    const result = await response.json();    return result as AnalysisResult;  } catch (error) {    console.error('Backend API Error:', error);    throw new Error(      error instanceof Error        ? error.message        : 'Failed to connect to backend API. Please ensure the backend server is running.'    );  }}/** * Parse resume to extract skills and summary using backend API */export async function parseResumeWithBackend(  resumeText: string): Promise<{ skills: string[]; summary: string }> {  try {    const response = await fetch(`${API_BASE_URL}${API_PREFIX}/seeker/parse-resume`, {      method: 'POST',      headers: {        'Content-Type': 'application/json',      },      body: JSON.stringify({        resume_text: resumeText,      }),    });    if (!response.ok) {      const errorData = await response.json().catch(() => ({}));      throw new Error(        errorData.error?.message || `Backend API error: ${response.status}`      );    }    const result = await response.json();    return result;  } catch (error) {    console.error('Backend API Error:', error);    throw new Error(      error instanceof Error        ? error.message        : 'Failed to parse resume. Please ensure the backend server is running.'    );  }}/** * Rank multiple candidates against job description using backend API */export async function rankCandidatesWithBackend(  jobDescription: string,  candidates: CandidateData[]): Promise<RankingResult> {  try {    const response = await fetch(`${API_BASE_URL}${API_PREFIX}/recruiter/rank-candidates`, {      method: 'POST',      headers: {        'Content-Type': 'application/json',      },      body: JSON.stringify({        jobDescription: jobDescription,        candidates: candidates,      }),    });    if (!response.ok) {      const errorData = await response.json().catch(() => ({}));      throw new Error(        errorData.error?.message || `Backend API error: ${response.status}`      );    }    const result = await response.json();    return result as RankingResult;  } catch (error) {    console.error('Backend API Error:', error);    throw new Error(      error instanceof Error        ? error.message        : 'Failed to rank candidates. Please ensure the backend server is running.'    );  }}/** * Check backend API health */export async function checkBackendHealth(): Promise<boolean> {  try {    const response = await fetch(`${API_BASE_URL}/health`, {      method: 'GET',    });    return response.ok;  } catch (error) {    console.error('Backend health check failed:', error);    return false;  }}/** * Get backend API base URL */export function getBackendUrl(): string {  return API_BASE_URL;}// Made with Bob