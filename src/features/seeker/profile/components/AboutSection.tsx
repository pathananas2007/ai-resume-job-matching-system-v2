import React from 'react';import { UserProfile } from '../types/profile';import { GraduationCap, Briefcase, Folder, Award } from 'lucide-react';interface AboutSectionProps {  profile: UserProfile;}export function AboutSection({ profile }: AboutSectionProps) {  return (    <div className="bg-white rounded-xl border border-slate-200 p-6 md:p-8 space-y-10">      <section>        <h3 className="text-lg font-bold text-slate-900 mb-4">About Me</h3>        <p className="text-slate-600 leading-relaxed">{profile.bio}</p>      </section>      <section>        <h3 className="text-lg font-bold text-slate-900 mb-6 flex items-center gap-2">          <Briefcase className="w-5 h-5 text-blue-600" />          Experience        </h3>        <div className="space-y-6">          {profile.experience.map((exp, index) => (            <div key={index} className="relative pl-6 border-l-2 border-slate-100">              <div className="absolute w-3 h-3 bg-blue-600 rounded-full -left-[7px] top-1.5" />              <h4 className="font-semibold text-slate-900">{exp.role}</h4>              <p className="text-sm text-slate-500 mb-2">{exp.company} ΓÇó {exp.duration}</p>              <p className="text-sm text-slate-600">{exp.description}</p>            </div>          ))}        </div>      </section>      <section>        <h3 className="text-lg font-bold text-slate-900 mb-6 flex items-center gap-2">          <GraduationCap className="w-5 h-5 text-blue-600" />          Education        </h3>        <div className="space-y-6">          {profile.education.map((edu, index) => (            <div key={index} className="relative pl-6 border-l-2 border-slate-100">              <div className="absolute w-3 h-3 bg-slate-300 rounded-full -left-[7px] top-1.5" />              <h4 className="font-semibold text-slate-900">{edu.degree}</h4>              <p className="text-sm text-slate-500">{edu.school} ΓÇó {edu.year}</p>            </div>          ))}        </div>      </section>      <section>        <h3 className="text-lg font-bold text-slate-900 mb-6 flex items-center gap-2">          <Folder className="w-5 h-5 text-blue-600" />          Projects        </h3>        <div className="grid md:grid-cols-2 gap-4">          {profile.projects.map((project, index) => (            <div key={index} className="p-4 rounded-xl border border-slate-200 hover:border-blue-200 hover:shadow-sm transition-all bg-slate-50/50">              <h4 className="font-semibold text-slate-900 mb-2">{project.name}</h4>              <p className="text-sm text-slate-600 mb-4">{project.description}</p>              <a href={`https://${project.link}`} target="_blank" rel="noreferrer" className="text-sm text-blue-600 hover:underline font-medium">                {project.link}              </a>            </div>          ))}        </div>      </section>      <section>        <h3 className="text-lg font-bold text-slate-900 mb-4 flex items-center gap-2">          <Award className="w-5 h-5 text-blue-600" />          Certifications        </h3>        <div className="flex flex-wrap gap-2">          {profile.certifications.map((cert, index) => (            <span key={index} className="px-3 py-1.5 bg-slate-100 text-slate-700 rounded-lg text-sm font-medium border border-slate-200">              {cert}            </span>          ))}        </div>      </section>    </div>  );}