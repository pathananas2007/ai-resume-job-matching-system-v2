import React from 'react';import { UserProfile } from '../types/profile';import { Github, Linkedin, Globe, ExternalLink } from 'lucide-react';interface SocialLinksCardProps {  profile: UserProfile;}export function SocialLinksCard({ profile }: SocialLinksCardProps) {  const links = [    { name: 'GitHub', icon: <Github className="w-5 h-5 text-slate-700" />, url: profile.socialLinks.github },    { name: 'LinkedIn', icon: <Linkedin className="w-5 h-5 text-blue-600" />, url: profile.socialLinks.linkedin },    { name: 'Portfolio', icon: <Globe className="w-5 h-5 text-purple-600" />, url: profile.socialLinks.portfolio },    { name: 'Website', icon: <Globe className="w-5 h-5 text-emerald-600" />, url: profile.socialLinks.website },  ].filter(link => !!link.url);  return (    <div className="bg-white rounded-xl border border-slate-200 p-6">      <h3 className="font-bold text-slate-900 mb-6">Social Links</h3>            <div className="space-y-3">        {links.length > 0 ? links.map((link) => (          <a            key={link.name}            href={link.url}            target="_blank"            rel="noopener noreferrer"            className="flex items-center justify-between p-3 rounded-lg border border-slate-200 hover:border-blue-200 hover:bg-slate-50 transition-colors group"          >            <div className="flex items-center gap-3">              {link.icon}              <span className="text-sm font-medium text-slate-700 group-hover:text-blue-700">{link.name}</span>            </div>            <ExternalLink className="w-4 h-4 text-slate-400 group-hover:text-blue-600 transition-colors" />          </a>        )) : (          <p className="text-sm text-slate-500">No social links added yet.</p>        )}      </div>    </div>  );}