import React, { useState } from 'react';import { motion, AnimatePresence } from 'motion/react';import { Bookmark, MapPin, Clock, Briefcase, DollarSign, Sparkles, CheckCircle2, AlertCircle, Send, ChevronDown, ChevronUp, ArrowRight } from 'lucide-react';import { cn } from '../../../../lib/utils';import { Job } from '../data/mockJobs';import { useJobStore } from '../store/useJobStore';import { ApplyModal } from './ApplyModal';import { useNavigate } from 'react-router-dom';interface JobCardProps {  job: Job;}export function JobCard({ job }: JobCardProps) {  const [showMatchDetails, setShowMatchDetails] = useState(false);  const [isApplyOpen, setIsApplyOpen] = useState(false);  const toggleSavedJob = useJobStore(state => state.toggleSavedJob);  const savedJobIds = useJobStore(state => state.savedJobIds);  const isSaved = savedJobIds.has(job.id);  const navigate = useNavigate();  const handleCardClick = () => {    navigate(`/seeker/jobs/${job.id}`);  };  const handleSave = (e: React.MouseEvent) => {    e.stopPropagation();    toggleSavedJob(job.id);  };  const handleApply = (e: React.MouseEvent) => {    e.stopPropagation();    setIsApplyOpen(true);  };  const getMatchColor = (score: number) => {    if (score >= 85) return 'text-green-600 bg-green-50 border-green-200 dark:bg-green-900/20 dark:border-green-900/50 dark:text-green-400';    if (score >= 70) return 'text-[#1e3a8a] bg-blue-50 border-blue-200 dark:bg-blue-900/20 dark:border-blue-900/50 dark:text-blue-400';    return 'text-amber-600 bg-amber-50 border-amber-200 dark:bg-amber-900/20 dark:border-amber-900/50 dark:text-amber-400';  };  return (    <>      <motion.div         initial={{ opacity: 0, y: 10 }}        animate={{ opacity: 1, y: 0 }}        className="bg-white dark:bg-slate-900 rounded-2xl border border-slate-200 dark:border-slate-800 overflow-hidden hover:shadow-md transition-all cursor-pointer group"        onClick={handleCardClick}      >        <div className="p-5">          <div className="flex justify-between items-start gap-4 mb-4">            <div className="flex items-start gap-4">              <div className="w-12 h-12 rounded-xl bg-slate-50 dark:bg-slate-800 border border-slate-100 dark:border-slate-700 flex items-center justify-center text-xl font-bold text-slate-400 shrink-0">                {job.company.logo}              </div>              <div>                <div className="flex flex-wrap gap-2 mb-1.5">                  <span className="text-sm font-semibold text-slate-600 dark:text-slate-400">{job.company.name}</span>                  {job.isTopMatch && <span className="px-2 py-0.5 bg-purple-100 text-purple-700 dark:bg-purple-900/30 dark:text-purple-400 text-[10px] font-bold rounded-full uppercase tracking-wide flex items-center gap-1"><Sparkles className="w-3 h-3" /> Top Match</span>}                  {job.isUrgent && <span className="px-2 py-0.5 bg-red-100 text-red-700 dark:bg-red-900/30 dark:text-red-400 text-[10px] font-bold rounded-full uppercase tracking-wide flex items-center gap-1"><AlertCircle className="w-3 h-3" /> Urgent Hiring</span>}                  {job.isNew && <span className="px-2 py-0.5 bg-blue-100 text-blue-700 dark:bg-blue-900/30 dark:text-blue-400 text-[10px] font-bold rounded-full uppercase tracking-wide">New</span>}                </div>                <h3 className="text-lg font-bold text-slate-900 dark:text-white mb-1 group-hover:text-[#1e3a8a] dark:group-hover:text-blue-400 transition-colors">{job.title}</h3>                <div className="flex flex-wrap items-center gap-3 text-sm text-slate-500 dark:text-slate-400">                  <span className="flex items-center gap-1"><MapPin className="w-3.5 h-3.5" />{job.location}</span>                  <span className="flex items-center gap-1"><Briefcase className="w-3.5 h-3.5" />{job.level} ┬╖ {job.type}</span>                  <span className="flex items-center gap-1"><DollarSign className="w-3.5 h-3.5" />{job.salary}</span>                  <span className="flex items-center gap-1"><Clock className="w-3.5 h-3.5" />{job.posted}</span>                </div>              </div>            </div>            <button               onClick={handleSave}              className={cn("p-2 rounded-lg transition-colors border", isSaved ? "bg-blue-50 border-blue-200 text-[#1e3a8a] dark:bg-blue-900/20 dark:border-blue-800 dark:text-blue-400" : "bg-white border-slate-200 text-slate-400 hover:text-[#1e3a8a] hover:border-blue-200 hover:bg-blue-50 dark:bg-slate-900 dark:border-slate-800 dark:hover:border-blue-800 dark:hover:bg-blue-900/20")}            >              <Bookmark className="w-4 h-4" fill={isSaved ? "currentColor" : "none"} />            </button>          </div>          {/* AI Match Overview */}          <div className="mt-5 p-4 rounded-xl bg-slate-50 dark:bg-slate-800/50 border border-slate-100 dark:border-slate-800">            <div               className="flex items-center justify-between cursor-pointer"              onClick={(e) => { e.stopPropagation(); setShowMatchDetails(!showMatchDetails); }}            >              <div className="flex items-center gap-3">                <div className={cn("px-2.5 py-1 rounded-lg border font-bold text-sm", getMatchColor(job.match.score))}>                  {job.match.score}% Match                </div>                <p className="text-sm font-medium text-slate-700 dark:text-slate-300">                  {job.match.matchingSkills.length} matching skills, {job.match.missingSkills.length} missing                </p>              </div>              <button className="text-slate-400 hover:text-slate-600 dark:hover:text-slate-300">                {showMatchDetails ? <ChevronUp className="w-5 h-5" /> : <ChevronDown className="w-5 h-5" />}              </button>            </div>            <AnimatePresence>              {showMatchDetails && (                <motion.div                  initial={{ height: 0, opacity: 0 }}                  animate={{ height: 'auto', opacity: 1 }}                  exit={{ height: 0, opacity: 0 }}                  className="overflow-hidden"                >                  <div className="pt-4 mt-4 border-t border-slate-200 dark:border-slate-700 grid sm:grid-cols-2 gap-4">                    <div>                      <p className="text-xs font-semibold text-green-600 dark:text-green-500 uppercase tracking-wider mb-3 flex items-center gap-1.5"><CheckCircle2 className="w-4 h-4" /> Matching Skills</p>                      <ul className="space-y-2">                        {job.match.matchingSkills.map(skill => (                          <li key={skill} className="flex items-start gap-2 text-sm text-slate-700 dark:text-slate-300 font-medium">                            <span className="text-green-500 font-bold shrink-0">Γ£ô</span> {skill}                          </li>                        ))}                      </ul>                    </div>                    <div>                      <p className="text-xs font-semibold text-amber-600 dark:text-amber-500 uppercase tracking-wider mb-3 flex items-center gap-1.5"><AlertCircle className="w-4 h-4" /> Missing Skills</p>                      <ul className="space-y-3">                        {job.match.missingSkills.map(skill => {                          const recommendation = job.match.learningRecommendations.find(r => r.skill === skill);                          return (                            <li key={skill} className="flex flex-col gap-1">                              <div className="flex items-start gap-2 text-sm text-slate-700 dark:text-slate-300 font-medium">                                <span className="text-amber-500 font-bold shrink-0">ΓÜá</span> {skill}                              </div>                              {recommendation && (                                <div className="ml-5">                                  <a                                     href={recommendation.link}                                     onClick={(e) => e.stopPropagation()}                                    className="inline-flex items-center gap-1 text-xs font-semibold text-[#1e3a8a] hover:text-blue-700 dark:text-blue-400 dark:hover:text-blue-300 transition-colors"                                  >                                    View Resource <ArrowRight className="w-3 h-3" />                                  </a>                                </div>                              )}                            </li>                          );                        })}                      </ul>                    </div>                  </div>                </motion.div>              )}            </AnimatePresence>          </div>          <div className="mt-5 flex gap-3">            <button               onClick={handleApply}              className="flex-1 inline-flex justify-center items-center gap-2 bg-[#0d1b2a] hover:bg-[#0a1628] text-white font-semibold py-2.5 rounded-xl transition-colors"            >              <Send className="w-4 h-4" /> Quick Apply            </button>            <button               onClick={handleCardClick}              className="px-6 py-2.5 rounded-xl border border-slate-200 dark:border-slate-700 font-semibold text-slate-700 dark:text-slate-300 hover:bg-slate-50 dark:hover:bg-slate-800 transition-colors"            >              View Details            </button>          </div>        </div>      </motion.div>      <ApplyModal job={job} isOpen={isApplyOpen} onClose={() => setIsApplyOpen(false)} />    </>  );}