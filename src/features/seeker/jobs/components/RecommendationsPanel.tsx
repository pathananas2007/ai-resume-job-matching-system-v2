import React from 'react';import { motion } from 'motion/react';import { Sparkles, TrendingUp, History, ArrowRight } from 'lucide-react';import { useJobStore } from '../store/useJobStore';import { useNavigate } from 'react-router-dom';export function RecommendationsPanel() {  const jobs = useJobStore(state => state.jobs);  const recentlyViewedIds = useJobStore(state => state.recentlyViewedIds);  const navigate = useNavigate();  const topMatches = [...jobs].sort((a, b) => b.match.score - a.match.score).slice(0, 3);  const trendingJobs = [...jobs].filter(j => j.isUrgent || j.insights.applicants > 100).slice(0, 3);  const recentlyViewed = recentlyViewedIds.map(id => jobs.find(j => j.id === id)).filter(Boolean).slice(0, 5);  return (    <div className="space-y-6">      {/* Top Matches Widget */}      <div className="bg-white dark:bg-slate-900 rounded-xl border border-slate-200 dark:border-slate-800 p-5">        <div className="flex items-center justify-between mb-4">          <h3 className="text-sm font-bold text-slate-900 dark:text-white uppercase tracking-wider flex items-center gap-2">            <Sparkles className="w-4 h-4 text-purple-500" />            Top Matches          </h3>        </div>        <div className="space-y-3">          {topMatches.map(job => (            <div               key={job.id}               onClick={() => navigate(`/seeker/jobs/${job.id}`)}              className="flex items-start gap-3 p-3 rounded-lg hover:bg-slate-50 dark:hover:bg-slate-800/50 cursor-pointer transition-colors border border-transparent hover:border-slate-100 dark:hover:border-slate-800"            >              <div className="w-8 h-8 rounded-lg bg-slate-100 dark:bg-slate-800 flex items-center justify-center text-xs font-bold text-slate-500 shrink-0">                {job.company.logo}              </div>              <div className="flex-1 min-w-0">                <p className="text-sm font-semibold text-slate-900 dark:text-white truncate">{job.title}</p>                <p className="text-xs text-slate-500 dark:text-slate-400 truncate">{job.company.name}</p>              </div>              <span className="text-xs font-bold text-green-600 dark:text-green-500">{job.match.score}%</span>            </div>          ))}        </div>      </div>      {/* Trending Jobs Widget */}      <div className="bg-white dark:bg-slate-900 rounded-xl border border-slate-200 dark:border-slate-800 p-5">        <h3 className="text-sm font-bold text-slate-900 dark:text-white uppercase tracking-wider flex items-center gap-2 mb-4">          <TrendingUp className="w-4 h-4 text-blue-500" />          Trending Now        </h3>        <div className="space-y-3">          {trendingJobs.map(job => (            <div               key={job.id}               onClick={() => navigate(`/seeker/jobs/${job.id}`)}              className="flex items-start gap-3 p-3 rounded-lg hover:bg-slate-50 dark:hover:bg-slate-800/50 cursor-pointer transition-colors border border-transparent hover:border-slate-100 dark:hover:border-slate-800"            >              <div className="w-8 h-8 rounded-lg bg-slate-100 dark:bg-slate-800 flex items-center justify-center text-xs font-bold text-slate-500 shrink-0">                {job.company.logo}              </div>              <div className="flex-1 min-w-0">                <p className="text-sm font-semibold text-slate-900 dark:text-white truncate">{job.title}</p>                <p className="text-xs text-slate-500 dark:text-slate-400 truncate">{job.company.name}</p>              </div>            </div>          ))}        </div>      </div>      {/* Recently Viewed Widget */}      {recentlyViewed.length > 0 && (        <div className="bg-white dark:bg-slate-900 rounded-xl border border-slate-200 dark:border-slate-800 p-5">          <h3 className="text-sm font-bold text-slate-900 dark:text-white uppercase tracking-wider flex items-center gap-2 mb-4">            <History className="w-4 h-4 text-slate-400" />            Recently Viewed          </h3>          <div className="space-y-3">            {recentlyViewed.map(job => job && (              <div                 key={job.id}                 onClick={() => navigate(`/seeker/jobs/${job.id}`)}                className="flex items-start gap-3 p-3 rounded-lg hover:bg-slate-50 dark:hover:bg-slate-800/50 cursor-pointer transition-colors border border-transparent hover:border-slate-100 dark:hover:border-slate-800"              >                <div className="w-8 h-8 rounded-lg bg-slate-100 dark:bg-slate-800 flex items-center justify-center text-xs font-bold text-slate-500 shrink-0">                  {job.company.logo}                </div>                <div className="flex-1 min-w-0">                  <p className="text-sm font-semibold text-slate-900 dark:text-white truncate">{job.title}</p>                  <p className="text-xs text-slate-500 dark:text-slate-400 truncate">{job.company.name}</p>                </div>              </div>            ))}          </div>        </div>      )}    </div>  );}