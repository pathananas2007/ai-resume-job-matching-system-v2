import React, { useState } from 'react';import { motion } from 'motion/react';import { Search, Bookmark, MapPin, Briefcase, Trash2, Send, AlertCircle } from 'lucide-react';import { useJobStore } from './store/useJobStore';import { ApplyModal } from './components/ApplyModal';import { useNavigate } from 'react-router-dom';import { Job } from './data/mockJobs';const USE_MOCK_DATA = import.meta.env.VITE_USE_MOCK_DATA === 'true';export default function SavedJobsPage() {  const { jobs, savedJobIds, toggleSavedJob } = useJobStore();  const [search, setSearch] = useState('');  const [timeFilter, setTimeFilter] = useState('All Time');  const [selectedJobToApply, setSelectedJobToApply] = useState<Job | null>(null);  const navigate = useNavigate();  const savedJobs = jobs.filter(j => savedJobIds.has(j.id));    const filteredJobs = savedJobs.filter(j => {    const matchesSearch = !search ||       j.title.toLowerCase().includes(search.toLowerCase()) ||       j.company.name.toLowerCase().includes(search.toLowerCase());          /* Mock time filtering logic (since we don't have actual saved dates yet) */ let matchesTime = true;    if (timeFilter === 'Saved Today') {      matchesTime = Math.random() > 0.5; /* Mock data logic */ } else if (timeFilter === 'Saved This Week') {      matchesTime = Math.random() > 0.2; /* Mock data logic */ }        return matchesSearch && matchesTime;  });  return (    <div className="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-8 animate-fade-in">      <div className="mb-8">        <h1 className="text-2xl font-bold text-slate-900 dark:text-white">          Saved Jobs          <span className="ml-2 text-sm font-normal text-amber-600 bg-amber-50 dark:bg-amber-900/20 dark:text-amber-400 px-2 py-1 rounded">            {USE_MOCK_DATA ? 'Demo Data' : 'Locally Saved'}          </span>        </h1>        <p className="text-slate-500 dark:text-slate-400 mt-1">Review and apply to jobs you've bookmarked.</p>      </div>      <div className="bg-white dark:bg-slate-900 rounded-2xl border border-slate-200 dark:border-slate-800 overflow-hidden shadow-sm">        <div className="p-4 border-b border-slate-200 dark:border-slate-800 flex flex-col sm:flex-row gap-4">          <div className="relative flex-1">            <Search className="absolute left-3 top-1/2 -translate-y-1/2 w-4 h-4 text-slate-400" />            <input               type="text"               placeholder="Search saved jobs..."              className="w-full pl-9 pr-4 py-2 bg-slate-50 dark:bg-slate-800/50 border border-slate-200 dark:border-slate-700 rounded-lg text-sm focus:outline-none focus:ring-2 focus:ring-blue-500/20 focus:border-blue-500 transition-all dark:text-white"              value={search}               onChange={e => setSearch(e.target.value)}             />          </div>          <select             value={timeFilter}            onChange={(e) => setTimeFilter(e.target.value)}            className="px-4 py-2 bg-white dark:bg-slate-900 border border-slate-200 dark:border-slate-700 rounded-lg text-sm focus:outline-none focus:ring-2 focus:ring-blue-500/20 focus:border-blue-500 transition-all dark:text-white"          >            <option>All Time</option>            <option>Saved Today</option>            <option>Saved This Week</option>          </select>        </div>        {savedJobs.length === 0 ? (          <div className="p-16 text-center">            <div className="w-16 h-16 bg-blue-50 dark:bg-blue-900/20 text-blue-500 rounded-full flex items-center justify-center mx-auto mb-4">              <Bookmark className="w-8 h-8" />            </div>            <h3 className="text-lg font-bold text-slate-900 dark:text-white mb-2">No Saved Jobs</h3>            <p className="text-slate-500 dark:text-slate-400 max-w-sm mx-auto mb-6">You haven't saved any jobs yet. Browse the job board and save jobs to review later.</p>            <button onClick={() => navigate('/seeker/jobs')} className="px-6 py-2.5 rounded-xl bg-blue-600 text-white font-semibold hover:bg-blue-700 transition-colors">              Browse Jobs            </button>          </div>        ) : filteredJobs.length === 0 ? (          <div className="p-16 text-center text-slate-500 dark:text-slate-400">            No saved jobs match your search.          </div>        ) : (          <div className="overflow-x-auto">            <table className="w-full text-left border-collapse">              <thead>                <tr className="bg-slate-50 dark:bg-slate-800/50 border-b border-slate-200 dark:border-slate-800">                  <th className="px-6 py-3 text-xs font-semibold text-slate-500 dark:text-slate-400 uppercase tracking-wider">Job</th>                  <th className="px-6 py-3 text-xs font-semibold text-slate-500 dark:text-slate-400 uppercase tracking-wider">Match %</th>                  <th className="px-6 py-3 text-xs font-semibold text-slate-500 dark:text-slate-400 uppercase tracking-wider">Missing Skills</th>                  <th className="px-6 py-3 text-xs font-semibold text-slate-500 dark:text-slate-400 uppercase tracking-wider text-right">Actions</th>                </tr>              </thead>              <tbody className="divide-y divide-slate-100 dark:divide-slate-800">                {filteredJobs.map(job => (                  <motion.tr                     key={job.id}                    initial={{ opacity: 0 }}                    animate={{ opacity: 1 }}                    className="hover:bg-slate-50 dark:hover:bg-slate-800/50 transition-colors"                  >                    <td className="px-6 py-4">                      <div                         className="flex items-center gap-4 cursor-pointer"                        onClick={() => navigate(`/seeker/jobs/${job.id}`)}                      >                        <div className="w-10 h-10 rounded-lg bg-slate-100 dark:bg-slate-800 border border-slate-200 dark:border-slate-700 flex items-center justify-center text-lg font-bold text-slate-500">                          {job.company.logo}                        </div>                        <div>                          <p className="font-semibold text-slate-900 dark:text-white hover:text-blue-600 dark:hover:text-blue-400 transition-colors">{job.title}</p>                          <div className="flex items-center gap-2 text-xs text-slate-500 dark:text-slate-400 mt-1">                            <span className="font-medium text-slate-700 dark:text-slate-300">{job.company.name}</span>                            <span>┬╖</span>                            <span className="flex items-center gap-1"><MapPin className="w-3 h-3" />{job.location}</span>                            <span>┬╖</span>                            <span>Saved {new Date().toLocaleDateString()}</span>                          </div>                        </div>                      </div>                    </td>                    <td className="px-6 py-4">                      <span className={`inline-flex px-2.5 py-1 rounded-md text-xs font-bold border ${job.match.score >= 85 ? 'bg-green-50 text-green-700 border-green-200 dark:bg-green-900/20 dark:border-green-900/50 dark:text-green-400' : job.match.score >= 70 ? 'bg-blue-50 text-blue-700 border-blue-200 dark:bg-blue-900/20 dark:border-blue-900/50 dark:text-blue-400' : 'bg-amber-50 text-amber-700 border-amber-200 dark:bg-amber-900/20 dark:border-amber-900/50 dark:text-amber-400'}`}>                        {job.match.score}%                      </span>                    </td>                    <td className="px-6 py-4">                      {job.match.missingSkills.length > 0 ? (                        <div className="flex flex-wrap gap-1.5">                          {job.match.missingSkills.slice(0, 2).map(skill => (                            <span key={skill} className="px-2 py-0.5 rounded bg-slate-100 text-slate-600 text-xs font-medium dark:bg-slate-800 dark:text-slate-300">{skill}</span>                          ))}                          {job.match.missingSkills.length > 2 && (                            <span className="px-2 py-0.5 rounded bg-slate-100 text-slate-600 text-xs font-medium dark:bg-slate-800 dark:text-slate-300">+{job.match.missingSkills.length - 2} more</span>                          )}                        </div>                      ) : (                        <span className="text-xs text-slate-400">ΓÇö</span>                      )}                    </td>                    <td className="px-6 py-4 text-right">                      <div className="flex items-center justify-end gap-2">                        <button                           onClick={() => toggleSavedJob(job.id)}                          className="p-2 text-slate-400 hover:text-red-600 hover:bg-red-50 rounded-lg transition-colors dark:hover:bg-red-900/20"                          title="Remove"                        >                          <Trash2 className="w-4 h-4" />                        </button>                        <button                           onClick={() => setSelectedJobToApply(job)}                          className="px-4 py-2 bg-blue-600 hover:bg-blue-700 text-white text-sm font-semibold rounded-lg transition-colors flex items-center gap-2"                        >                          <Send className="w-3.5 h-3.5" /> Quick Apply                        </button>                      </div>                    </td>                  </motion.tr>                ))}              </tbody>            </table>          </div>        )}      </div>      {selectedJobToApply && (        <ApplyModal           job={selectedJobToApply}           isOpen={true}           onClose={() => setSelectedJobToApply(null)}         />      )}    </div>  );}