import { create } from 'zustand';import { Application } from '../types/application';import { MOCK_APPLICATIONS } from '../data/mockApplications';interface ApplicationState {  applications: Application[];  searchQuery: string;  statusFilter: string;  sortBy: string;  viewMode: 'list' | 'kanban';    setSearchQuery: (query: string) => void;  setStatusFilter: (status: string) => void;  setSortBy: (sort: string) => void;  setViewMode: (mode: 'list' | 'kanban') => void;    getApplicationById: (id: string) => Application | undefined;  updateApplicationStatus: (id: string, newStatus: Application['status']) => void;}export const useApplicationStore = create<ApplicationState>((set, get) => ({  applications: MOCK_APPLICATIONS,  searchQuery: '',  statusFilter: 'all',  sortBy: 'latest',  viewMode: 'list',  setSearchQuery: (query) => set({ searchQuery: query }),  setStatusFilter: (status) => set({ statusFilter: status }),  setSortBy: (sort) => set({ sortBy: sort }),  setViewMode: (mode) => set({ viewMode: mode }),  getApplicationById: (id) => {    return get().applications.find((app) => app.id === id);  },  updateApplicationStatus: (id, newStatus) => {    set((state) => ({      applications: state.applications.map((app) =>        app.id === id ? { ...app, status: newStatus } : app      ),    }));  },}));