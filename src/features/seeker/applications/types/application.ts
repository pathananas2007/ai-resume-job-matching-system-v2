export type ApplicationStatus = 'applied' | 'under review' | 'interview' | 'selected' | 'rejected';export interface AIRecommendation {  missingSkills: string[];  learningSuggestions: { title: string; link: string }[];  expectedMatchImprovement: number;  currentMatch: number;}export interface ApplicationDocument {  id: string;  type: 'resume' | 'cover_letter' | 'portfolio' | 'linkedin';  name: string;  url: string;}export interface ApplicationNote {  id: string;  text: string;  date: string;}export interface InterviewDetails {  id: string;  date: string;  time: string;  mode: string;  recruiter: string;  meetingLink: string;  status: 'upcoming' | 'completed' | 'cancelled';  topics: string[];}export interface TimelineEvent {  id: string;  status: ApplicationStatus | string;  label: string;  date: string;  completed: boolean;  current: boolean;}export interface Application {  id: string;  company: {    name: string;    logo: string;  };  role: string;  status: ApplicationStatus;  appliedDate: string;  matchScore: number;  remote: boolean;  type: string;  salary: string;  nextStep?: string;  timeline: TimelineEvent[];  interviews: InterviewDetails[];  notes: ApplicationNote[];  documents: ApplicationDocument[];  aiRecommendations?: AIRecommendation;}