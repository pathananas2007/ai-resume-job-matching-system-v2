import React, { useState } from 'react';import { StickyNote, Plus, Send } from 'lucide-react';import { ApplicationNote } from '../types/application';interface NotesCardProps {  notes: ApplicationNote[];}export function NotesCard({ notes }: NotesCardProps) {  const [newNote, setNewNote] = useState('');  return (    <div className="bg-white rounded-xl border border-slate-200 p-6">      <div className="flex items-center justify-between mb-6">        <h3 className="font-bold text-slate-900 flex items-center gap-2">          <StickyNote className="w-5 h-5 text-[#1e3a8a]" />          My Notes        </h3>        <button className="text-sm font-medium text-[#1e3a8a] hover:text-blue-700 flex items-center gap-1">          <Plus className="w-4 h-4" /> Add Note        </button>      </div>      <div className="space-y-4 mb-6">        {notes.length === 0 ? (          <div className="text-center py-6 text-sm text-slate-500 bg-slate-50 rounded-lg border border-dashed border-slate-200">            No notes added yet.          </div>        ) : (          notes.map((note) => (            <div key={note.id} className="bg-yellow-50/50 border border-yellow-100 rounded-lg p-4">              <p className="text-sm text-slate-700 mb-2">{note.text}</p>              <p className="text-xs text-slate-400">{note.date}</p>            </div>          ))        )}      </div>      <div className="relative">        <textarea          value={newNote}          onChange={(e) => setNewNote(e.target.value)}          placeholder="Type a new note..."          className="w-full pl-4 pr-12 py-3 bg-slate-50 border border-slate-200 rounded-xl text-sm focus:outline-none focus:ring-2 focus:ring-blue-500/20 focus:border-blue-500 resize-none h-20"        />        <button          className="absolute right-3 bottom-3 p-1.5 bg-[#0d1b2a] text-white rounded-lg hover:bg-[#0a1628] transition-colors disabled:opacity-50 disabled:cursor-not-allowed"          disabled={!newNote.trim()}        >          <Send className="w-4 h-4" />        </button>      </div>    </div>  );}