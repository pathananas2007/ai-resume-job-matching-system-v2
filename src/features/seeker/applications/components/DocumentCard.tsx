import React from 'react';import { FileText, Download, Link as LinkIcon, File } from 'lucide-react';import { ApplicationDocument } from '../types/application';interface DocumentCardProps {  documents: ApplicationDocument[];}const getIconForType = (type: string) => {  switch (type) {    case 'resume':    case 'cover_letter':      return <FileText className="w-5 h-5 text-blue-600" />;    case 'portfolio':    case 'linkedin':      return <LinkIcon className="w-5 h-5 text-purple-600" />;    default:      return <File className="w-5 h-5 text-slate-600" />;  }};export function DocumentCard({ documents }: DocumentCardProps) {  return (    <div className="bg-white rounded-xl border border-slate-200 p-6">      <h3 className="font-bold text-slate-900 mb-6">Documents Included</h3>            <div className="space-y-3">        {documents.map((doc) => (          <div key={doc.id} className="flex items-center justify-between p-3 border border-slate-200 rounded-lg hover:bg-slate-50 transition-colors group">            <div className="flex items-center gap-3">              <div className={`p-2 rounded-lg ${doc.type === 'resume' || doc.type === 'cover_letter' ? 'bg-blue-50' : 'bg-purple-50'}`}>                {getIconForType(doc.type)}              </div>              <div>                <p className="text-sm font-medium text-slate-900">{doc.name}</p>                <p className="text-xs text-slate-500 capitalize">{doc.type.replace('_', ' ')}</p>              </div>            </div>                        <a              href={doc.url}              target={doc.type === 'portfolio' || doc.type === 'linkedin' ? '_blank' : '_self'}              rel="noopener noreferrer"              className="p-2 text-slate-400 hover:text-blue-600 hover:bg-blue-50 rounded-lg transition-colors"            >              {doc.type === 'portfolio' || doc.type === 'linkedin' ? (                <LinkIcon className="w-4 h-4" />              ) : (                <Download className="w-4 h-4" />              )}            </a>          </div>        ))}      </div>    </div>  );}