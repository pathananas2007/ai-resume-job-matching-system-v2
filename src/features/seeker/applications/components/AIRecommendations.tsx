import React from 'react';import { Sparkles, TrendingUp, BookOpen, Target } from 'lucide-react';import { useNavigate } from 'react-router-dom';import { AIRecommendation } from '../types/application';interface AIRecommendationsProps {  recommendations?: AIRecommendation;}export function AIRecommendations({ recommendations }: AIRecommendationsProps) {  const navigate = useNavigate();  if (!recommendations) return null;  return (    <div className="bg-gradient-to-br from-indigo-50 to-blue-50 rounded-xl border border-indigo-100 p-6 relative overflow-hidden">      <div className="absolute top-0 right-0 p-4 opacity-10">        <Sparkles className="w-24 h-24 text-indigo-600" />      </div>      <div className="relative z-10">        <h3 className="font-bold text-indigo-900 flex items-center gap-2 mb-6">          <Sparkles className="w-5 h-5 text-indigo-600" />          AI Insights & Recommendations        </h3>        <div className="grid md:grid-cols-2 gap-6 mb-6">          <div className="bg-white/60 backdrop-blur-sm rounded-lg p-4 border border-white/40">            <div className="flex items-center gap-2 text-sm font-medium text-indigo-800 mb-2">              <Target className="w-4 h-4" /> Match Analysis            </div>            <div className="flex items-end gap-3">              <div className="text-3xl font-bold text-indigo-600">{recommendations.currentMatch}%</div>              <div className="text-sm text-slate-500 mb-1">Current Match</div>            </div>            <div className="mt-2 flex items-center gap-1.5 text-xs font-medium text-emerald-600">              <TrendingUp className="w-3 h-3" />              Potential to reach {recommendations.currentMatch + recommendations.expectedMatchImprovement}%            </div>          </div>          <div className="bg-white/60 backdrop-blur-sm rounded-lg p-4 border border-white/40">            <div className="flex items-center gap-2 text-sm font-medium text-indigo-800 mb-3">              Missing Skills            </div>            <div className="flex flex-wrap gap-2">              {recommendations.missingSkills.map((skill) => (                <span                  key={skill}                  className="px-2.5 py-1 bg-white border border-indigo-100 text-indigo-700 rounded-md text-xs font-medium shadow-sm"                >                  {skill}                </span>              ))}            </div>          </div>        </div>        <div>          <h4 className="text-sm font-medium text-indigo-900 mb-3">Recommended Learning</h4>          <div className="space-y-2">            {recommendations.learningSuggestions.map((suggestion, i) => (              <div key={i} className="flex items-center justify-between bg-white rounded-lg p-3 border border-indigo-50 shadow-sm">                <div className="flex items-center gap-3">                  <div className="w-8 h-8 rounded-md bg-indigo-50 flex items-center justify-center">                    <BookOpen className="w-4 h-4 text-indigo-600" />                  </div>                  <span className="text-sm font-medium text-slate-700">{suggestion.title}</span>                </div>                <button                  onClick={() => navigate(suggestion.link)}                  className="text-xs font-medium text-indigo-600 hover:text-indigo-800 bg-indigo-50 hover:bg-indigo-100 px-3 py-1.5 rounded-md transition-colors"                >                  Start Learning                </button>              </div>            ))}          </div>        </div>        <div className="mt-6 flex justify-end">          <button            onClick={() => navigate('/seeker/skills')}            className="text-sm font-medium text-white bg-indigo-600 hover:bg-indigo-700 px-4 py-2 rounded-lg transition-colors shadow-sm"          >            Go to Skill Gap Analysis          </button>        </div>      </div>    </div>  );}