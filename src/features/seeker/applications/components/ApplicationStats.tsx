import React from 'react';import { motion } from 'motion/react';import { Briefcase, Clock, Calendar, CheckCircle } from 'lucide-react';import { useApplicationStore } from '../store/useApplicationStore';export function ApplicationStats() {  const applications = useApplicationStore((state) => state.applications);  const stats = [    {      label: 'Total Applications',      value: applications.length,      icon: <Briefcase className="w-5 h-5 text-blue-600" />,      color: 'bg-blue-50 border-blue-100 text-blue-600',      trend: '+2 this week',    },    {      label: 'Under Review',      value: applications.filter((a) => a.status === 'under review').length,      icon: <Clock className="w-5 h-5 text-yellow-600" />,      color: 'bg-yellow-50 border-yellow-100 text-yellow-600',      trend: '1 action needed',    },    {      label: 'Interviews',      value: applications.filter((a) => a.status === 'interview').length,      icon: <Calendar className="w-5 h-5 text-purple-600" />,      color: 'bg-purple-50 border-purple-100 text-purple-600',      trend: '2 upcoming',    },    {      label: 'Offers',      value: applications.filter((a) => a.status === 'selected').length,      icon: <CheckCircle className="w-5 h-5 text-green-600" />,      color: 'bg-green-50 border-green-100 text-green-600',      trend: '1 accepted',    },  ];  return (    <div className="grid grid-cols-2 lg:grid-cols-4 gap-4">      {stats.map((stat, i) => (        <motion.div          key={stat.label}          initial={{ opacity: 0, y: 10 }}          animate={{ opacity: 1, y: 0 }}          transition={{ delay: i * 0.1 }}          className="bg-white rounded-xl border border-slate-200 p-5 shadow-sm hover:shadow-md transition-shadow"        >          <div className="flex items-center justify-between mb-4">            <div className={`w-10 h-10 rounded-lg flex items-center justify-center ${stat.color}`}>              {stat.icon}            </div>          </div>          <div className="space-y-1">            <h3 className="text-3xl font-bold text-slate-900">{stat.value}</h3>            <p className="text-sm font-medium text-slate-500">{stat.label}</p>          </div>          <div className="mt-4 text-xs font-medium text-slate-400">            {stat.trend}          </div>        </motion.div>      ))}    </div>  );}