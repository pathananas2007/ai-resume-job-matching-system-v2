import React from 'react';import { CheckCircle2, Circle } from 'lucide-react';import { TimelineEvent } from '../types/application';import { cn } from '../../../../lib/utils';interface ApplicationTimelineProps {  timeline: TimelineEvent[];}export function ApplicationTimeline({ timeline }: ApplicationTimelineProps) {  return (    <div className="bg-white rounded-xl border border-slate-200 p-6">      <h3 className="font-bold text-slate-900 mb-6">Application Status</h3>            <div className="relative">        <div className="absolute left-[11px] top-3 bottom-3 w-0.5 bg-slate-100" />                <div className="space-y-6">          {timeline.map((event, index) => (            <div key={event.id} className="relative flex items-start gap-4">              <div className="relative z-10 bg-white">                {event.completed ? (                  <CheckCircle2 className="w-6 h-6 text-blue-600" />                ) : event.current ? (                  <div className="w-6 h-6 rounded-full border-2 border-blue-600 flex items-center justify-center">                    <div className="w-2 h-2 rounded-full bg-blue-600" />                  </div>                ) : (                  <Circle className="w-6 h-6 text-slate-300" />                )}              </div>                            <div className={cn("flex-1 pb-1", !event.completed && !event.current && "opacity-50")}>                <h4 className={cn("font-medium", event.completed || event.current ? "text-slate-900" : "text-slate-500")}>                  {event.label}                </h4>                {event.date !== 'TBD' && (                  <p className="text-sm text-slate-500 mt-0.5">{event.date}</p>                )}              </div>            </div>          ))}        </div>      </div>    </div>  );}