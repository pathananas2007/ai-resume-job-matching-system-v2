import React, { useState } from 'react';import { UserSettings } from '../types/settings';import { Moon, Sun, Monitor, LayoutTemplate } from 'lucide-react';import { cn } from '../../../../lib/utils';import { userApi } from '../../../../services/api/userApi';interface AppearanceTabProps {  settings: UserSettings;}export function AppearanceTab({ settings }: AppearanceTabProps) {  const [theme, setTheme] = useState(settings.appearance.theme);  const [accent, setAccent] = useState(settings.appearance.accentColor);  const [compact, setCompact] = useState(settings.appearance.compactMode);  const [isSaving, setIsSaving] = useState(false);  const handleThemeChange = async (newTheme: string) => {    setTheme(newTheme);    setIsSaving(true);    try {      await userApi.updateAppearance({        theme: newTheme,        accentColor: accent,        compactMode: compact,      });    } catch (error) {      setTheme(settings.appearance.theme);      console.error('Failed to save theme preference', error);    } finally {      setIsSaving(false);    }  };  const handleAccentChange = async (newAccent: string) => {    setAccent(newAccent);    setIsSaving(true);    try {      await userApi.updateAppearance({        theme,        accentColor: newAccent,        compactMode: compact,      });    } catch (error) {      setAccent(settings.appearance.accentColor);      console.error('Failed to save accent preference', error);    } finally {      setIsSaving(false);    }  };  const handleCompactChange = async () => {    const newCompact = !compact;    setCompact(newCompact);    setIsSaving(true);    try {      await userApi.updateAppearance({        theme,        accentColor: accent,        compactMode: newCompact,      });    } catch (error) {      setCompact(settings.appearance.compactMode);      console.error('Failed to save compact mode preference', error);    } finally {      setIsSaving(false);    }  };  return (    <div className="bg-white rounded-xl border border-slate-200 p-6 space-y-8">      <div>        <h3 className="font-bold text-slate-900 mb-4">Theme</h3>        <div className="grid grid-cols-3 gap-4">          <button            onClick={() => handleThemeChange('light')}            disabled={isSaving}            className={cn(              "flex flex-col items-center gap-3 p-4 rounded-xl border-2 transition-all disabled:opacity-50 disabled:cursor-not-allowed",              theme === 'light' ? "border-[#1e3a8a] bg-blue-50/50" : "border-slate-200 hover:border-slate-300"            )}          >            <div className="w-full h-24 rounded-lg bg-slate-100 border border-slate-200 flex items-center justify-center">              <Sun className="w-8 h-8 text-slate-400" />            </div>            <span className="font-medium text-slate-900">Light</span>          </button>                    <button            onClick={() => handleThemeChange('dark')}            disabled={isSaving}            className={cn(              "flex flex-col items-center gap-3 p-4 rounded-xl border-2 transition-all disabled:opacity-50 disabled:cursor-not-allowed",              theme === 'dark' ? "border-[#1e3a8a] bg-blue-50/50" : "border-slate-200 hover:border-slate-300"            )}          >            <div className="w-full h-24 rounded-lg bg-slate-900 border border-slate-800 flex items-center justify-center">              <Moon className="w-8 h-8 text-slate-500" />            </div>            <span className="font-medium text-slate-900">Dark</span>          </button>          <button            onClick={() => handleThemeChange('system')}            disabled={isSaving}            className={cn(              "flex flex-col items-center gap-3 p-4 rounded-xl border-2 transition-all disabled:opacity-50 disabled:cursor-not-allowed",              theme === 'system' ? "border-[#1e3a8a] bg-blue-50/50" : "border-slate-200 hover:border-slate-300"            )}          >            <div className="w-full h-24 rounded-lg bg-gradient-to-br from-slate-100 to-slate-800 border border-slate-300 flex items-center justify-center">              <Monitor className="w-8 h-8 text-white mix-blend-difference" />            </div>            <span className="font-medium text-slate-900">System</span>          </button>        </div>      </div>      <div>        <h3 className="font-bold text-slate-900 mb-4">Accent Color</h3>        <div className="flex gap-4">          <button            onClick={() => handleAccentChange('blue')}            disabled={isSaving}            className={cn(              "w-12 h-12 rounded-full bg-[#0d1b2a] border-4 transition-all disabled:opacity-50 disabled:cursor-not-allowed",              accent === 'blue' ? "border-blue-200 shadow-md transform scale-110" : "border-white"            )}          />          <button            onClick={() => handleAccentChange('cyan')}            disabled={isSaving}            className={cn(              "w-12 h-12 rounded-full bg-cyan-600 border-4 transition-all disabled:opacity-50 disabled:cursor-not-allowed",              accent === 'cyan' ? "border-cyan-200 shadow-md transform scale-110" : "border-white"            )}          />          <button            onClick={() => handleAccentChange('purple')}            disabled={isSaving}            className={cn(              "w-12 h-12 rounded-full bg-purple-600 border-4 transition-all disabled:opacity-50 disabled:cursor-not-allowed",              accent === 'purple' ? "border-purple-200 shadow-md transform scale-110" : "border-white"            )}          />        </div>      </div>      <div className="flex items-center justify-between pt-4 border-t border-slate-100">        <div>          <h3 className="font-bold text-slate-900 flex items-center gap-2">            <LayoutTemplate className="w-5 h-5 text-slate-500" />            Compact Mode          </h3>          <p className="text-sm text-slate-500 mt-1">Reduce spacing to fit more content on the screen.</p>        </div>        <label className="relative inline-flex items-center cursor-pointer">          <input             type="checkbox"             className="sr-only peer"             checked={compact}            onChange={handleCompactChange}            disabled={isSaving}          />          <div className={cn(            "w-11 h-6 peer-focus:outline-none peer-focus:ring-4 peer-focus:ring-blue-300 rounded-full peer peer-checked:after:translate-x-full peer-checked:after:border-white after:content-[''] after:absolute after:top-[2px] after:left-[2px] after:bg-white after:border-slate-300 after:border after:rounded-full after:h-5 after:w-5 after:transition-all peer-checked:bg-[#0d1b2a]",            isSaving && "opacity-50 cursor-not-allowed",            !isSaving && "bg-slate-200"          )}></div>        </label>      </div>    </div>  );}