export interface UserSettings {  general: {    name: string;    email: string;    phone: string;    location: string;    timezone: string;  };  security: {    twoFactorEnabled: boolean;    activeSessions: {      device: string;      location: string;      lastActive: string;      isCurrent: boolean;    }[];  };  notifications: {    email: boolean;    push: boolean;    jobAlerts: boolean;    learningReminders: boolean;    applicationUpdates: boolean;    interviewReminders: boolean;    resumeAnalysis: boolean;  };  appearance: {    theme: 'light' | 'dark' | 'system';    accentColor: 'blue' | 'cyan' | 'purple';    compactMode: boolean;  };  privacy: {    profileVisibility: 'public' | 'recruiters' | 'private';    showResume: boolean;    enableAnalytics: boolean;  };}