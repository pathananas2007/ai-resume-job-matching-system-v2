import { useState, useCallback, useEffect } from 'react';import { ApiError } from '../types/api';interface UseApiState<T> {  data: T | null;  isLoading: boolean;  error: ApiError | null;}interface UseApiOptions {  onSuccess?: (data: any) => void;  onError?: (error: ApiError) => void;  immediate?: boolean;}export function useApi<T>(apiFunc: (...args: any[]) => Promise<T>, options: UseApiOptions = {}) {  const { immediate = false, onSuccess, onError } = options;  const [state, setState] = useState<UseApiState<T>>({    data: null,    isLoading: immediate,    error: null,  });  const execute = useCallback(    async (...args: any[]) => {      setState((prev) => ({ ...prev, isLoading: true, error: null }));      try {        const response = await apiFunc(...args);        setState({ data: response, isLoading: false, error: null });        onSuccess?.(response);        return response;      } catch (err: any) {        const parsedError: ApiError = err?.error || {          code: 'UNKNOWN_ERROR',          message: err?.message || 'An unexpected error occurred',        };        setState({ data: null, isLoading: false, error: parsedError });        onError?.(parsedError);        throw parsedError;      }    },    [apiFunc, onSuccess, onError]  );  useEffect(() => {    if (immediate) {      execute();    }  }, [execute, immediate]);  return {    ...state,    execute,    setData: (data: T | null) => setState((prev) => ({ ...prev, data })),  };}