from fastapi import APIRouter, Depends, HTTPException, statusfrom fastapi.security import OAuth2PasswordRequestFormfrom fastapi.responses import RedirectResponsefrom app.core.database import get_databasefrom app.core.security import get_password_hash, verify_password, create_access_token, get_current_userfrom app.models.user import UserModel, Rolefrom pydantic import BaseModelfrom .oauth import (    GoogleOAuth, GitHubOAuth, OAuthState, OAuthUser, find_or_create_user)router = APIRouter()class UserRegister(BaseModel):    full_name: str    email: str    password: str    role: Role = Role.JOB_SEEKER@router.post("/register")async def register(payload: UserRegister):    db = get_database()    existing = await db["users"].find_one({"email": payload.email})    if existing:        raise HTTPException(status_code=400, detail="Email already registered")        user_doc = payload.dict(exclude={"password"})    user_doc["name"] = user_doc.pop("full_name", payload.full_name)  # Map full_name to name    user_doc["hashed_password"] = get_password_hash(payload.password)        result = await db["users"].insert_one(user_doc)    user_doc["_id"] = str(result.inserted_id)        # Generate token for automatic login after registration    access_token = create_access_token(subject=str(user_doc["_id"]))        return {"access_token": access_token, "token_type": "bearer"}@router.post("/login")async def login(form_data: OAuth2PasswordRequestForm = Depends()):    db = get_database()    user_doc = await db["users"].find_one({"email": form_data.username})    if not user_doc:        raise HTTPException(status_code=400, detail="Incorrect email or password")        # Verify password ΓÇö handle missing or invalid hash    stored_hash = user_doc.get("hashed_password", "")    if not stored_hash:        # No password set for this user (OAuth-only account or legacy issue)        raise HTTPException(status_code=400, detail="Incorrect email or password")        try:        if not verify_password(form_data.password, stored_hash):            raise HTTPException(status_code=400, detail="Incorrect email or password")    except Exception:        # Password hash is invalid/corrupted        raise HTTPException(status_code=400, detail="Incorrect email or password")        access_token = create_access_token(subject=str(user_doc["_id"]))    return {"access_token": access_token, "token_type": "bearer"}@router.get("/me", response_model=UserModel)async def get_current_user_info(current_user: UserModel = Depends(get_current_user)):    """Get current authenticated user information"""    return current_user# ΓöÇΓöÇΓöÇ GOOGLE OAUTH ΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇ@router.get("/oauth/google/authorize")async def google_authorize():    """Initiate Google OAuth flow"""    if not GoogleOAuth.is_configured():        raise HTTPException(status_code=503, detail="Google OAuth is not configured")        state = OAuthState.generate()    auth_url = GoogleOAuth.get_authorization_url(state)        return {"auth_url": auth_url, "state": state}@router.get("/oauth/google/callback")async def google_callback(code: str, state: str):    """Handle Google OAuth callback"""    if not GoogleOAuth.is_configured():        raise HTTPException(status_code=503, detail="Google OAuth is not configured")        # Validate state to prevent CSRF    if not OAuthState.validate(state):        raise HTTPException(status_code=400, detail="Invalid or expired state parameter. Please try signing in again.")        OAuthState.consume(state)        # Exchange code for token    access_token = await GoogleOAuth.exchange_code(code)    if not access_token:        raise HTTPException(status_code=400, detail="Failed to exchange authorization code. Please try again.")        # Get user info    oauth_user = await GoogleOAuth.get_user_info(access_token)    if not oauth_user or not oauth_user.email:        raise HTTPException(status_code=400, detail="Failed to retrieve your Google account information.")        # Find or create user    user = await find_or_create_user(oauth_user)    if not user:        raise HTTPException(status_code=500, detail="Failed to create user account")        # Generate Elevara JWT    elevara_token = create_access_token(subject=str(user.id))        # Redirect to frontend with token    return RedirectResponse(        url=f"http://localhost:3001/auth/callback?token={elevara_token}&provider=google"    )# ΓöÇΓöÇΓöÇ GITHUB OAUTH ΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇ@router.get("/oauth/github/authorize")async def github_authorize():    """Initiate GitHub OAuth flow"""    if not GitHubOAuth.is_configured():        raise HTTPException(status_code=503, detail="GitHub OAuth is not configured")        state = OAuthState.generate()    auth_url = GitHubOAuth.get_authorization_url(state)        return {"auth_url": auth_url, "state": state}@router.get("/oauth/github/callback")async def github_callback(code: str, state: str):    """Handle GitHub OAuth callback"""    if not GitHubOAuth.is_configured():        raise HTTPException(status_code=503, detail="GitHub OAuth is not configured")        # Validate state to prevent CSRF    if not OAuthState.validate(state):        raise HTTPException(status_code=400, detail="Invalid or expired state parameter. Please try signing in again.")        OAuthState.consume(state)        # Exchange code for token    access_token = await GitHubOAuth.exchange_code(code)    if not access_token:        raise HTTPException(status_code=400, detail="Failed to exchange authorization code. Please try again.")        # Get user info    oauth_user = await GitHubOAuth.get_user_info(access_token)    if not oauth_user or not oauth_user.email:        raise HTTPException(status_code=400, detail="Failed to retrieve your GitHub account information.")        # Find or create user    user = await find_or_create_user(oauth_user)    if not user:        raise HTTPException(status_code=500, detail="Failed to create user account")        # Generate Elevara JWT    elevara_token = create_access_token(subject=str(user.id))        # Redirect to frontend with token    return RedirectResponse(        url=f"http://localhost:3001/auth/callback?token={elevara_token}&provider=github"    )