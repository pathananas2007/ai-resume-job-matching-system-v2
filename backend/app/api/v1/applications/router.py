from fastapi import APIRouter, Depends, HTTPException, Query, statusfrom typing import Optionalfrom app.core.security import get_current_user, require_rolefrom app.models.applications import ApplicationStatusfrom app.models.user import Role, UserModelfrom app.schemas.applications import (    ApplicationCreate,    ApplicationInterviewUpdate,    ApplicationListResponse,    ApplicationNotesUpdate,    ApplicationResponse,    ApplicationStatusUpdate,)from app.services.application_service import (    create_application,    get_application,    list_applications,    update_application_status,    withdraw_application,    update_interview,    update_applicant_notes,)from app.services.job_service import get_jobrouter = APIRouter()# ΓöÇΓöÇΓöÇ POST /applications ΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇ@router.post("", response_model=ApplicationResponse, status_code=status.HTTP_201_CREATED)async def create_application_endpoint(    payload: ApplicationCreate,    current_user: UserModel = Depends(get_current_user),    _: None = Depends(require_role(Role.JOB_SEEKER)),):    """Job seeker applies to a job. applicant_id and recruiter_id are derived server-side."""    # Fetch job to get recruiter_id    job = await get_job(payload.job_id)    if not job:        raise HTTPException(status_code=404, detail="Job not found")    if not job.is_active:        raise HTTPException(status_code=410, detail="Job is no longer accepting applications")    try:        application = await create_application(            applicant_id=current_user.id,            recruiter_id=job.recruiter_id,            payload=payload,        )    except ValueError as e:        if "duplicate" in str(e):            raise HTTPException(                status_code=status.HTTP_409_CONFLICT,                detail="You have already applied to this job.",            )        raise HTTPException(status_code=400, detail=str(e))    return application# ΓöÇΓöÇΓöÇ GET /applications (seeker: own applications) ΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇ@router.get("", response_model=ApplicationListResponse)async def list_my_applications_endpoint(    status: Optional[ApplicationStatus] = None,    job_id: Optional[str] = None,    page: int = Query(1, ge=1),    page_size: int = Query(20, ge=1, le=100),    current_user: UserModel = Depends(get_current_user),    _: None = Depends(require_role(Role.JOB_SEEKER, Role.ADMIN)),):    """List applications belonging to the authenticated job seeker."""    applicant_id = current_user.id if current_user.role == Role.JOB_SEEKER else None    items, total = await list_applications(        applicant_id=applicant_id,        job_id=job_id,        status=status,        page=page,        page_size=page_size,    )    total_pages = max(1, (total + page_size - 1) // page_size)    return ApplicationListResponse(        items=items,        page=page,        page_size=page_size,        total=total,        total_pages=total_pages,    )# ΓöÇΓöÇΓöÇ GET /applications/recruiter (recruiter: view applications for their jobs) @router.get("/recruiter", response_model=ApplicationListResponse)async def list_recruiter_applications_endpoint(    job_id: Optional[str] = None,    status: Optional[ApplicationStatus] = None,    page: int = Query(1, ge=1),    page_size: int = Query(20, ge=1, le=100),    current_user: UserModel = Depends(get_current_user),    _: None = Depends(require_role(Role.RECRUITER, Role.ADMIN)),):    """List all applications for jobs posted by this recruiter."""    recruiter_id = current_user.id if current_user.role == Role.RECRUITER else None    items, total = await list_applications(        recruiter_id=recruiter_id,        job_id=job_id,        status=status,        page=page,        page_size=page_size,    )    total_pages = max(1, (total + page_size - 1) // page_size)    return ApplicationListResponse(        items=items,        page=page,        page_size=page_size,        total=total,        total_pages=total_pages,    )# ΓöÇΓöÇΓöÇ GET /applications/{application_id} ΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇ@router.get("/{application_id}", response_model=ApplicationResponse)async def get_application_endpoint(    application_id: str,    current_user: UserModel = Depends(get_current_user),):    """Retrieve a single application. Seeker can only see their own."""    application = await get_application(application_id)    if not application:        raise HTTPException(status_code=404, detail="Application not found")    # Access control: seekers only see their own; recruiters only see their jobs; admins see all    if current_user.role == Role.JOB_SEEKER and application.applicant_id != current_user.id:        raise HTTPException(status_code=404, detail="Application not found")    if current_user.role == Role.RECRUITER and application.recruiter_id != current_user.id:        raise HTTPException(status_code=403, detail="Not authorized")    return application# ΓöÇΓöÇΓöÇ PATCH /applications/{application_id}/status ΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇ@router.patch("/{application_id}/status", response_model=ApplicationResponse)async def update_status_endpoint(    application_id: str,    payload: ApplicationStatusUpdate,    current_user: UserModel = Depends(get_current_user),    _: None = Depends(require_role(Role.RECRUITER, Role.ADMIN)),):    """Recruiter or admin updates application status."""    application = await get_application(application_id)    if not application:        raise HTTPException(status_code=404, detail="Application not found")    if current_user.role == Role.RECRUITER and application.recruiter_id != current_user.id:        raise HTTPException(status_code=403, detail="Not authorized")    updated = await update_application_status(application_id, payload.status)    if not updated:        raise HTTPException(status_code=404, detail="Application not found")    return updated# ΓöÇΓöÇΓöÇ POST /applications/{application_id}/withdraw ΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇ@router.post("/{application_id}/withdraw", response_model=ApplicationResponse)async def withdraw_application_endpoint(    application_id: str,    current_user: UserModel = Depends(get_current_user),    _: None = Depends(require_role(Role.JOB_SEEKER)),):    """Job seeker withdraws their own application."""    application = await get_application(application_id)    if not application:        raise HTTPException(status_code=404, detail="Application not found")    if application.applicant_id != current_user.id:        raise HTTPException(status_code=403, detail="Not authorized")    if application.status == ApplicationStatus.WITHDRAWN:        raise HTTPException(status_code=409, detail="Application already withdrawn")    updated = await withdraw_application(application_id, current_user.id)    if not updated:        raise HTTPException(status_code=404, detail="Application not found")    return updated# ΓöÇΓöÇΓöÇ PATCH /applications/{application_id}/interview ΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇ@router.patch("/{application_id}/interview", response_model=ApplicationResponse)async def update_interview_endpoint(    application_id: str,    payload: ApplicationInterviewUpdate,    current_user: UserModel = Depends(get_current_user),    _: None = Depends(require_role(Role.RECRUITER, Role.ADMIN)),):    """Recruiter sets interview details."""    application = await get_application(application_id)    if not application:        raise HTTPException(status_code=404, detail="Application not found")    if current_user.role == Role.RECRUITER and application.recruiter_id != current_user.id:        raise HTTPException(status_code=403, detail="Not authorized")    updated = await update_interview(        application_id,        payload.interview_date,        payload.interview_time,        payload.interview_mode,        payload.interview_link,    )    if not updated:        raise HTTPException(status_code=404, detail="Application not found")    return updated# ΓöÇΓöÇΓöÇ PATCH /applications/{application_id}/notes ΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇ@router.patch("/{application_id}/notes", response_model=ApplicationResponse)async def update_notes_endpoint(    application_id: str,    payload: ApplicationNotesUpdate,    current_user: UserModel = Depends(get_current_user),    _: None = Depends(require_role(Role.JOB_SEEKER)),):    """Job seeker updates their own applicant notes."""    application = await get_application(application_id)    if not application:        raise HTTPException(status_code=404, detail="Application not found")    if application.applicant_id != current_user.id:        raise HTTPException(status_code=403, detail="Not authorized")    updated = await update_applicant_notes(application_id, current_user.id, payload.applicant_notes)    if not updated:        raise HTTPException(status_code=404, detail="Application not found")    return updated