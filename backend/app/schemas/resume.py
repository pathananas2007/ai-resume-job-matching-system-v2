from datetime import datetimefrom typing import List, Optionalfrom pydantic import BaseModel, Field, ConfigDictfrom app.models.resumes import ResumeStatus, AnalysisStatusclass ResumeResponse(BaseModel):    id: Optional[str] = Field(alias="_id")    user_id: str    file_name: str    file_type: str    file_size: int    original_file_name: str    mime_type: str        status: ResumeStatus    is_active: bool    deactivated_at: Optional[datetime] = None        analysis_status: AnalysisStatus    analysis_id: Optional[str] = None        uploaded_at: datetime    created_at: datetime    updated_at: datetime    model_config = ConfigDict(from_attributes=True, populate_by_name=True)class ResumeUploadResponse(ResumeResponse):    passclass ResumeListResponse(BaseModel):    items: List[ResumeResponse]    page: int    page_size: int    total: int    total_pages: int    model_config = ConfigDict(from_attributes=True)class ResumeUpdate(BaseModel):    # Only safe fields that the client is allowed to update    status: Optional[ResumeStatus] = None    is_active: Optional[bool] = None    model_config = ConfigDict(from_attributes=True)class ResumeAnalysisStatus(BaseModel):    analysis_status: AnalysisStatus    analysis_id: Optional[str] = None