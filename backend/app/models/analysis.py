"""MongoDB document model for persisted resume analysis results.Stored in the 'resume_analyses' collection."""from datetime import datetime, timezonefrom typing import List, Optionalfrom pydantic import Fieldfrom app.models.base import BaseDocumentModelfrom app.schemas.analysis import (    AnalysisProvider,    CertificationEntry,    EducationEntry,    ExperienceEntry,    ExtractedSkills,    OverallGrade,    PersonalInfo,    ProjectEntry,    SkillGapItem,)class ResumeAnalysisDocument(BaseDocumentModel):    """    Persisted analysis document. One per resume (upserted on re-analyze).    Fields mirror ResumeAnalysisResult plus storage metadata.    """    resume_id: str    user_id: str    # Core analysis    ats_score: int    overall_grade: OverallGrade    personal_info: PersonalInfo = Field(default_factory=PersonalInfo)    skills: ExtractedSkills = Field(default_factory=ExtractedSkills)    experience: List[ExperienceEntry] = Field(default_factory=list)    education: List[EducationEntry] = Field(default_factory=list)    projects: List[ProjectEntry] = Field(default_factory=list)    certifications: List[CertificationEntry] = Field(default_factory=list)    skill_gaps: List[SkillGapItem] = Field(default_factory=list)    recommendations: List[str] = Field(default_factory=list)    summary: Optional[str] = None    analysis_version: str = "1.0"    # Provenance    provider: AnalysisProvider = AnalysisProvider.MOCK    analyzed_at: datetime = Field(default_factory=lambda: datetime.now(timezone.utc))    # Error tracking (populated only when analysis fails)    error_reason: Optional[str] = None    error_code: Optional[str] = None