from datetime import datetime, timezonefrom enum import Enumfrom typing import Optional, Listfrom pydantic import Fieldfrom app.models.base import BaseDocumentModelclass ApplicationStatus(str, Enum):    APPLIED = "APPLIED"    UNDER_REVIEW = "UNDER_REVIEW"    SHORTLISTED = "SHORTLISTED"    INTERVIEW_SCHEDULED = "INTERVIEW_SCHEDULED"    FINAL_ROUND = "FINAL_ROUND"    SELECTED = "SELECTED"    REJECTED = "REJECTED"    WITHDRAWN = "WITHDRAWN"class ApplicationModel(BaseDocumentModel):    """MongoDB document model for job applications."""    job_id: str    applicant_id: str    recruiter_id: str  # denormalized from job at creation time    status: ApplicationStatus = ApplicationStatus.APPLIED    # Optional references    resume_id: Optional[str] = None    # Seeker-controlled fields    cover_note: Optional[str] = None    applicant_notes: Optional[str] = None  # private seeker notes    # Recruiter-controlled fields ΓÇö NEVER shown to seekers    recruiter_notes: Optional[str] = None    # AI matching ΓÇö computed asynchronously, may be null    match_score: Optional[float] = None    # Interview data ΓÇö set by recruiter    interview_date: Optional[datetime] = None    interview_time: Optional[str] = None    interview_mode: Optional[str] = None   # e.g. "Zoom", "Google Meet", "On-site"    interview_link: Optional[str] = None    # Audit timestamps    withdrawn_at: Optional[datetime] = None    updated_at: datetime = Field(default_factory=lambda: datetime.now(timezone.utc))    created_at: datetime = Field(default_factory=lambda: datetime.now(timezone.utc))