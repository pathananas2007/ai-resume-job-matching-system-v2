from datetime import datetime, timezonefrom enum import Enumfrom typing import Optional, Dict, Anyfrom pydantic import BaseModel, Fieldfrom app.models.base import BaseDocumentModelclass NotificationType(str, Enum):    RESUME = "RESUME"    APPLICATION = "APPLICATION"    LEARNING = "LEARNING"    JOB = "JOB"    INTERVIEW = "INTERVIEW"    SYSTEM = "SYSTEM"class NotificationModel(BaseDocumentModel):    user_id: str    type: NotificationType    title: str    message: str    is_read: bool = False    is_important: bool = False    action_url: Optional[str] = None    metadata: Optional[Dict[str, Any]] = None    created_at: datetime = Field(default_factory=lambda: datetime.now(timezone.utc))    updated_at: datetime = Field(default_factory=lambda: datetime.now(timezone.utc))