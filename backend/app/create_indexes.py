import loggingfrom motor.motor_asyncio import AsyncIOMotorClientfrom app.core.database import get_databaselogger = logging.getLogger(__name__)async def create_indexes():    """Create required MongoDB indexes for the jobs collection.    Handles existing indexes gracefully.    """    db = get_database()    collection = db["jobs"]    # Single-field indexes    indexes = [        ("recruiter_id", 1),        ("is_active", 1),        ("created_at", -1),        ("location", 1),        ("work_mode", 1),        ("employment_type", 1),        ("application_deadline", 1),    ]    for field, direction in indexes:        try:            await collection.create_index([(field, direction)], background=True)            logger.info(f"Created index on {field}")        except Exception as e:            logger.error(f"Failed to create index on {field}: {e}")    # Combined text index for search    try:        await collection.create_index(            [                ("title", "text"),                ("company_name", "text"),                ("skills", "text"),                ("location", "text"),                ("industry", "text"),            ],            name="combined_text_index",            background=True,        )        logger.info("Created combined text index for jobs")    except Exception as e:        logger.error(f"Failed to create combined text index: {e}")    # Notification collection indexes    notif_coll = db["notifications"]    try:        await notif_coll.create_index([("user_id", 1)], background=True)        logger.info("Created index on notifications.user_id")    except Exception as e:        logger.error(f"Failed to create index on notifications.user_id: {e}")    try:        await notif_coll.create_index([("type", 1)], background=True)        logger.info("Created index on notifications.type")    except Exception as e:        logger.error(f"Failed to create index on notifications.type: {e}")    try:        await notif_coll.create_index([("is_important", 1)], background=True)        logger.info("Created index on notifications.is_important")    except Exception as e:        logger.error(f"Failed to create index on notifications.is_important: {e}")    try:        # compound index for common query        await notif_coll.create_index([("user_id", 1), ("is_read", 1), ("created_at", -1)], background=True)        logger.info("Created compound index on notifications (user_id, is_read, created_at)")    except Exception as e:        logger.error(f"Failed to create compound index on notifications: {e}")    # Resume collection indexes    resumes_coll = db["resumes"]    try:        await resumes_coll.create_index([("user_id", 1), ("status", 1), ("created_at", -1)], background=True)        logger.info("Created compound index on resumes (user_id, status, created_at)")    except Exception as e:        logger.error(f"Failed to create compound index on resumes: {e}")