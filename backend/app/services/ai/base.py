"""Abstract base class for AI providers.Concrete implementations: MockProvider, GeminiProvider."""from abc import ABC, abstractmethodfrom typing import Optionalfrom app.schemas.analysis import ResumeAnalysisResult, ExtractedSkills, SkillGapItemclass AIProviderError(Exception):    """Raised when the AI provider fails in a controlled manner."""    def __init__(self, message: str, error_code: str = "AI_PROVIDER_ERROR"):        super().__init__(message)        self.error_code = error_codeclass AIProvider(ABC):    """    Provider-agnostic interface for AI operations.    Each method must be independently callable ΓÇö no shared state between calls.    """    @property    @abstractmethod    def provider_name(self) -> str:        """Identifier for this provider (e.g. 'gemini', 'mock')."""        ...    @abstractmethod    async def analyze_resume(        self,        text: str,        job_context: Optional[str] = None,    ) -> ResumeAnalysisResult:        """        Parse resume text and return structured analysis.        Args:            text: Normalized plain text of the resume.            job_context: Optional job title / description for targeted analysis.        Returns:            ResumeAnalysisResult with all fields populated.        Raises:            AIProviderError: On quota exhaustion, auth failure, or unrecoverable error.        """        ...    @abstractmethod    async def extract_skills(self, text: str) -> ExtractedSkills:        """        Extract and normalize skills from resume text.        Args:            text: Resume plain text.        Returns:            ExtractedSkills grouped by category.        Raises:            AIProviderError: On provider failure.        """        ...    @abstractmethod    async def compute_skill_gaps(        self,        resume_skills: ExtractedSkills,        target_role: str,    ) -> list[SkillGapItem]:        """        Compare resume skills against a target role and return gaps.        Args:            resume_skills: Skills already extracted from the resume.            target_role: Job title or role description.        Returns:            List of SkillGapItem with importance and reason.        Raises:            AIProviderError: On provider failure.        """        ...