"""Main FastAPI applicationAI Resume & Job Matching System BackendEnhanced with: response-time middleware, metrics tracking, improved health check"""import loggingimport timefrom contextlib import asynccontextmanagerfrom datetime import datetime, timezonefrom fastapi import FastAPI, Request, statusfrom fastapi.middleware.cors import CORSMiddlewarefrom fastapi.responses import JSONResponsefrom routes import seeker_routes, recruiter_routes, auth_routes, users_routesfrom utils.config import get_settingsfrom utils.logger import setup_logging# Setup logginglogger = setup_logging()# ΓöÇΓöÇ Metrics store (in-memory) ΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇclass Metrics:    def __init__(self) -> None:        self.total_requests: int = 0        self.total_errors: int = 0        self._response_times: list[float] = []        self.start_time: float = time.monotonic()    def record(self, duration_ms: float, is_error: bool) -> None:        self.total_requests += 1        if is_error:            self.total_errors += 1        self._response_times.append(duration_ms)        # Keep only last 1000 samples to bound memory        if len(self._response_times) > 1000:            self._response_times = self._response_times[-1000:]    @property    def avg_response_time_ms(self) -> float:        if not self._response_times:            return 0.0        return round(sum(self._response_times) / len(self._response_times), 2)    @property    def uptime_seconds(self) -> int:        return int(time.monotonic() - self.start_time)_metrics = Metrics()# ΓöÇΓöÇ Lifespan ΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇ@asynccontextmanagerasync def lifespan(app: FastAPI):    # Startup    logger.info("Starting AI Resume & Job Matching System Backend")    logger.info("Environment: %s", get_settings().environment)        # Connect to MongoDB on startup    from app.core.database import connect_to_mongo, close_mongo_connection    await connect_to_mongo()        yield        # Shutdown    await close_mongo_connection()    logger.info("Shutting down AI Resume & Job Matching System Backend")# ΓöÇΓöÇ App ΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇapp = FastAPI(    title="AI Resume & Job Matching System API",    description="Backend API for AI-powered resume analysis and job matching",    version="1.0.0",    lifespan=lifespan,    docs_url="/docs",    redoc_url="/redoc",    openapi_url="/openapi.json",)settings = get_settings()# CORSapp.add_middleware(    CORSMiddleware,    allow_origins=settings.origins_list if settings.origins_list else ["*"],    allow_credentials=True,    allow_methods=["*"],    allow_headers=["*"],    expose_headers=["*"],)# ΓöÇΓöÇ Response-time middleware ΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇ@app.middleware("http")async def response_time_middleware(request: Request, call_next):    start = time.perf_counter()    response = await call_next(request)    duration_ms = (time.perf_counter() - start) * 1000    is_error = response.status_code >= 400    _metrics.record(duration_ms, is_error)    response.headers["X-Response-Time"] = f"{duration_ms:.2f}ms"    logger.info(        "%s %s ΓåÆ %d (%.2fms)",        request.method,        request.url.path,        response.status_code,        duration_ms,    )    return response# ΓöÇΓöÇ Root ΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇ@app.get("/", status_code=status.HTTP_200_OK, tags=["Root"])async def root():    return {        "message": "AI Resume & Job Matching System API",        "version": "1.0.0",        "status": "operational",        "timestamp": datetime.now(timezone.utc).isoformat(),        "docs": "/docs",        "endpoints": {            "job_seeker": f"{settings.api_prefix}/seeker",            "recruiter": f"{settings.api_prefix}/recruiter",        },    }# ΓöÇΓöÇ Health check ΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇ@app.get("/health", status_code=status.HTTP_200_OK, tags=["Health"])async def health_check():    from services.gemini_service import get_gemini_service    # Quick Gemini connectivity probe    gemini_status = "unknown"    try:        svc = get_gemini_service()        # Just instantiating is enough to verify the key is present        gemini_status = "connected" if svc else "error"    except Exception as exc:        gemini_status = f"error: {exc}"    svc = get_gemini_service()    cache_stats = svc.cache_stats if svc else {}    return {        "status": "healthy",        "timestamp": datetime.now(timezone.utc).isoformat(),        "environment": settings.environment,        "version": "1.0.0",        "uptime_seconds": _metrics.uptime_seconds,        "gemini_api": gemini_status,        "cache": cache_stats,    }# ΓöÇΓöÇ Metrics ΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇ@app.get("/metrics", status_code=status.HTTP_200_OK, tags=["Metrics"])async def get_metrics():    from services.gemini_service import get_gemini_service    svc = get_gemini_service()    return {        "total_requests": _metrics.total_requests,        "total_errors": _metrics.total_errors,        "avg_response_time_ms": _metrics.avg_response_time_ms,        "uptime_seconds": _metrics.uptime_seconds,        "cache_hit_rate": svc.cache_stats.get("hit_rate", 0) if svc else 0,        "cache_size": svc.cache_stats.get("size", 0) if svc else 0,        "timestamp": datetime.now(timezone.utc).isoformat(),    }# ΓöÇΓöÇ Routers ΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇapp.include_router(seeker_routes.router, prefix=settings.api_prefix)app.include_router(recruiter_routes.router, prefix=settings.api_prefix)# Note: auth_routes is superseded by app.api.v1.auth.router which includes OAuth# Commented out to avoid route conflicts# app.include_router(auth_routes.router, prefix=settings.api_prefix)# app.include_router(users_routes.router, prefix=settings.api_prefix)  # Superseded by v1 router# Import the v1 API router that includes auth and userstry:    from app.api.v1.router import router as v1_router    app.include_router(v1_router, prefix=f"{settings.api_prefix}/v1")    logger.info("Successfully mounted v1 API router at %s/v1", settings.api_prefix)except ImportError as e:    logger.warning("V1 API router ImportError: %s, using legacy routes only", e)    logger.exception("Full traceback:")    app.include_router(users_routes.router, prefix=settings.api_prefix)except Exception as e:    logger.error("Failed to mount v1 API router: %s", e)    logger.exception("Full traceback:")    app.include_router(users_routes.router, prefix=settings.api_prefix)# ΓöÇΓöÇ Global exception handler ΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇ@app.exception_handler(Exception)async def global_exception_handler(request: Request, exc: Exception):    logger.error("Unhandled exception on %s %s: %s", request.method, request.url.path, exc, exc_info=True)    return JSONResponse(        status_code=status.HTTP_500_INTERNAL_SERVER_ERROR,        content={            "error": {                "message": "An unexpected error occurred",                "errorType": "InternalServerError",                "timestamp": datetime.now(timezone.utc).isoformat(),            }        },    )if __name__ == "__main__":    import uvicorn    uvicorn.run(        "main:app",        host=settings.host,        port=settings.port,        reload=not settings.is_production,        log_level="info",    )