# AI Resume & Job Matching System - Backend APIFastAPI backend for AI-powered resume analysis and job matching system.## ≡ƒÜÇ Features- **Job Seeker Analysis**: Analyze resumes against job descriptions with AI- **Recruiter Candidate Ranking**: Rank multiple candidates for a position- **Resume Parsing**: Extract skills and summary from resumes- **Structured AI Responses**: JSON-formatted responses with detailed insights- **Secure API**: Environment-based configuration with API key protection- **Production-Ready**: Modular architecture with proper error handling and logging## ≡ƒôü Project Structure```backend/Γö£ΓöÇΓöÇ main.py                 # FastAPI application entry pointΓö£ΓöÇΓöÇ requirements.txt        # Python dependenciesΓö£ΓöÇΓöÇ .env                    # Environment variables (not in git)Γö£ΓöÇΓöÇ .env.example           # Environment variables templateΓö£ΓöÇΓöÇ models/                # Pydantic models for validationΓöé   Γö£ΓöÇΓöÇ __init__.pyΓöé   ΓööΓöÇΓöÇ schemas.py         # Request/response schemasΓö£ΓöÇΓöÇ routes/                # API route handlersΓöé   Γö£ΓöÇΓöÇ __init__.pyΓöé   Γö£ΓöÇΓöÇ seeker_routes.py   # Job seeker endpointsΓöé   ΓööΓöÇΓöÇ recruiter_routes.py # Recruiter endpointsΓö£ΓöÇΓöÇ services/              # Business logic layerΓöé   Γö£ΓöÇΓöÇ __init__.pyΓöé   ΓööΓöÇΓöÇ gemini_service.py  # Gemini AI integrationΓö£ΓöÇΓöÇ prompts/               # AI prompt templatesΓöé   Γö£ΓöÇΓöÇ __init__.pyΓöé   Γö£ΓöÇΓöÇ resume_analysis_prompt.pyΓöé   ΓööΓöÇΓöÇ resume_parse_prompt.pyΓööΓöÇΓöÇ utils/                 # Utility modules    Γö£ΓöÇΓöÇ __init__.py    Γö£ΓöÇΓöÇ config.py          # Configuration management    ΓööΓöÇΓöÇ logger.py          # Logging setup```## ≡ƒ¢á∩╕Å Setup Instructions### Prerequisites- Python 3.9 or higher- pip (Python package manager)- Gemini API key from Google AI Studio### Installation1. **Navigate to backend directory**   ```bash   cd backend   ```2. **Create virtual environment**   ```bash   # Windows   python -m venv venv   venv\Scripts\activate   # macOS/Linux   python3 -m venv venv   source venv/bin/activate   ```3. **Install dependencies**   ```bash   pip install -r requirements.txt   ```4. **Configure environment variables**   ```bash   # Copy example env file   cp .env.example .env   # Edit .env and add your Gemini API key   # GEMINI_API_KEY=your_actual_api_key_here   ```5. **Run the server**   ```bash   # Development mode (with auto-reload)   python main.py   # Or using uvicorn directly   uvicorn main:app --reload --host 0.0.0.0 --port 8000   ```6. **Access the API**   - API Root: http://localhost:8000   - Interactive Docs: http://localhost:8000/docs   - ReDoc: http://localhost:8000/redoc## ≡ƒôí API Endpoints### Job Seeker Endpoints#### Analyze Resume```httpPOST /api/seeker/analyzeContent-Type: application/json{  "resume_text": "Your resume text here...",  "job_description": "Job description text here..."}```**Response**: Detailed analysis with match score, skills breakdown, strengths, weaknesses, and action plan.#### Parse Resume```httpPOST /api/seeker/parse-resumeContent-Type: application/json{  "resume_text": "Your resume text here..."}```**Response**: Extracted skills and professional summary.### Recruiter Endpoints#### Rank Candidates```httpPOST /api/recruiter/rank-candidatesContent-Type: application/json{  "jobDescription": "Job description text here...",  "candidates": [    {      "candidateId": "1",      "name": "John Doe",      "resumeText": "Resume text..."    }  ]}```**Response**: Ranked list of candidates with detailed scores.### Health Check```httpGET /health```## ≡ƒöº Configuration### Environment Variables| Variable | Description | Default ||----------|-------------|---------|| `GEMINI_API_KEY` | Google Gemini API key | Required || `HOST` | Server host | 0.0.0.0 || `PORT` | Server port | 8000 || `ENVIRONMENT` | Environment (development/production) | development || `ALLOWED_ORIGINS` | CORS allowed origins | localhost:3000,localhost:5173 || `API_PREFIX` | API route prefix | /api |## ≡ƒÅù∩╕Å Architecture### Layered Architecture1. **Routes Layer** (`routes/`)   - Handle HTTP requests/responses   - Input validation with Pydantic   - Error handling2. **Services Layer** (`services/`)   - Business logic   - AI integration   - Data processing3. **Models Layer** (`models/`)   - Request/response schemas   - Data validation   - Type safety4. **Prompts Layer** (`prompts/`)   - AI prompt templates   - Structured prompt generation5. **Utils Layer** (`utils/`)   - Configuration management   - Logging   - Helper functions### Key Design Patterns- **Dependency Injection**: Settings and services- **Singleton Pattern**: Gemini service instance- **Factory Pattern**: Prompt generation- **Repository Pattern**: Service layer abstraction## ≡ƒöÆ Security- API keys stored in environment variables- CORS configuration for frontend access- Input validation with Pydantic- Error handling without exposing internals- Production-ready logging## ≡ƒôè Logging- JSON logging in production- Structured logging with timestamps- Different log levels per environment- Request/response logging## ≡ƒº¬ Testing```bash# Run tests (when implemented)pytest# Run with coveragepytest --cov=.```## ≡ƒÜÇ Deployment### Production Checklist1. Set `ENVIRONMENT=production` in `.env`2. Use production Gemini API key3. Configure proper CORS origins4. Set up reverse proxy (nginx/Apache)5. Use process manager (systemd/supervisor)6. Enable HTTPS7. Set up monitoring and logging### Docker Deployment (Optional)```dockerfileFROM python:3.9-slimWORKDIR /appCOPY requirements.txt .RUN pip install --no-cache-dir -r requirements.txtCOPY . .CMD ["uvicorn", "main:app", "--host", "0.0.0.0", "--port", "8000"]```## ≡ƒöä Integration with FrontendUpdate your frontend to call the backend API:```typescript// Example: Call backend instead of direct Geminiconst response = await fetch('http://localhost:8000/api/seeker/analyze', {  method: 'POST',  headers: {    'Content-Type': 'application/json',  },  body: JSON.stringify({    resume_text: resumeText,    job_description: jobDescription,  }),});const result = await response.json();```## ≡ƒô¥ API Response FormatAll responses follow a consistent structure:### Success Response```json{  "score": 85,  "statusLabel": "≡ƒƒó STRONG MATCH ΓÇö 85/100 ΓÇö Recommend Interview",  "recommendation": "Interview",  "confidenceScore": 90,  "scores": {    "overall": 85,    "skills": 88,    "experience": 82,    "education": 85  },  ...}```### Error Response```json{  "error": {    "message": "Error description",    "errorType": "ErrorType",    "timestamp": "2024-01-01T00:00:00.000Z"  }}```## ≡ƒñ¥ Contributing1. Follow PEP 8 style guide2. Add type hints3. Write docstrings4. Update tests5. Update documentation## ≡ƒôä LicenseMIT License - See LICENSE file for details## ≡ƒæ¿ΓÇì≡ƒÆ╗ AuthorAnas Pathan## ≡ƒåÿ SupportFor issues and questions:- Check the API documentation at `/docs`- Review logs for error details- Ensure environment variables are set correctly- Verify Gemini API key is valid## ≡ƒö« Future Enhancements- [ ] Add caching for repeated analyses- [ ] Implement rate limiting- [ ] Add batch processing for large candidate pools- [ ] Add webhook support for async processing- [ ] Implement API authentication (JWT)- [ ] Add database integration for history- [ ] Add metrics and monitoring- [ ] Add unit and integration tests