"""List all available Gemini models"""import google.generativeai as genaifrom utils.config import get_settingsdef list_available_models():    """List all available Gemini models"""    try:        settings = get_settings()        genai.configure(api_key=settings.gemini_api_key)                print("Available Gemini Models:")        print("=" * 50)                for model in genai.list_models():            if 'generateContent' in model.supported_generation_methods:                print(f"Γ£à {model.name}")                print(f"   Display Name: {model.display_name}")                print(f"   Description: {model.description}")                print()            except Exception as e:        print(f"Error listing models: {e}")if __name__ == "__main__":    list_available_models()# Made with Bob