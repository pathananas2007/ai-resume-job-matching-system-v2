"""Quick test script to verify Gemini API key works"""import google.generativeai as genaifrom utils.config import get_settingsdef test_api_key():    """Test if the Gemini API key is valid"""    try:        settings = get_settings()        print(f"Testing API key: {settings.gemini_api_key[:20]}...")                genai.configure(api_key=settings.gemini_api_key)                model = genai.GenerativeModel('gemini-pro')                response = model.generate_content("Say 'API key works!'")                print("Γ£à SUCCESS! API key is valid!")        print(f"Response: {response.text}")        return True            except Exception as e:        print(f"Γ¥î FAILED! API key test failed:")        print(f"Error: {str(e)}")        return Falseif __name__ == "__main__":    test_api_key()# Made with Bob