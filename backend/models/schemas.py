"""Pydantic models for request/response validation"""from pydantic import BaseModel, Fieldfrom typing import List, Literal, Optional# ============================================================================# Job Seeker Analysis Models# ============================================================================class ResumeAnalysisRequest(BaseModel):    """Request model for resume analysis"""    resume_text: str = Field(..., min_length=50, description="Resume text content (minimum 50 characters)")    job_description: str = Field(..., min_length=50, description="Job description text (minimum 50 characters)")class ScoreBreakdown(BaseModel):    """Score breakdown model"""    overall: int = Field(..., ge=0, le=100)    skills: int = Field(..., ge=0, le=100)    experience: int = Field(..., ge=0, le=100)    education: int = Field(..., ge=0, le=100)class CompetencyAnalysis(BaseModel):    """Competency analysis model"""    category: str    candidate_score: int = Field(..., ge=0, le=10, alias="candidateScore")    required_score: int = Field(..., ge=0, le=10, alias="requiredScore")    gap: str        class Config:        populate_by_name = Trueclass SkillBreakdown(BaseModel):    """Skill breakdown model"""    skill: str    status: Literal["MATCH", "CLOSE", "GAP"]    candidate_level: int = Field(..., ge=0, le=10, alias="candidateLevel")    required_level: int = Field(..., ge=0, le=10, alias="requiredLevel")        class Config:        populate_by_name = Trueclass CriticalGap(BaseModel):    """Critical gap model"""    skill: str    classification: Literal["Critical", "Secondary"]    detail: strclass ActionPlan(BaseModel):    """Action plan model"""    skill: str    time_estimate: str = Field(..., alias="timeEstimate")    difficulty: Literal["Easy", "Medium", "Hard"]    impact: Literal["Low", "Medium", "High"]        class Config:        populate_by_name = Trueclass RadarData(BaseModel):    """Radar chart data model for UI compatibility"""    subject: str    A: int = Field(..., ge=0, le=10)    B: int = Field(..., ge=0, le=10)    full_mark: int = Field(default=10, alias="fullMark")        class Config:        populate_by_name = Trueclass ResumeAnalysisResponse(BaseModel):    """Response model for resume analysis"""    score: int = Field(..., ge=0, le=100)    status_label: str = Field(..., alias="statusLabel")    recommendation: Literal["Interview", "Consider", "Reject"]    confidence_score: int = Field(..., ge=0, le=100, alias="confidenceScore")    scores: ScoreBreakdown    competency_analysis: List[CompetencyAnalysis] = Field(..., alias="competencyAnalysis")    skill_breakdown: List[SkillBreakdown] = Field(..., alias="skillBreakdown")    matching_skills: List[str] = Field(..., alias="matchingSkills")    critical_gaps: List[CriticalGap] = Field(..., alias="criticalGaps")    strengths: List[str]    weaknesses: List[str]    action_plan: List[ActionPlan] = Field(..., alias="actionPlan")    explanation: str    radar_data: List[RadarData] = Field(..., alias="radarData")        class Config:        populate_by_name = True# ============================================================================# Recruiter Candidate Ranking Models# ============================================================================class CandidateData(BaseModel):    """Candidate data model"""    candidate_id: str = Field(..., alias="candidateId")    name: str    resume_text: str = Field(..., alias="resumeText")        class Config:        populate_by_name = Trueclass RecruiterRankingRequest(BaseModel):    """Request model for recruiter candidate ranking"""    job_description: str = Field(..., min_length=50, alias="jobDescription")    candidates: List[CandidateData] = Field(..., min_length=1, max_length=50)        class Config:        populate_by_name = Trueclass CandidateScore(BaseModel):    """Candidate score model"""    candidate_id: str = Field(..., alias="candidateId")    name: str    score: int = Field(..., ge=0, le=100)    status_label: str = Field(..., alias="statusLabel")    recommendation: Literal["Interview", "Consider", "Reject"]    confidence_score: int = Field(..., ge=0, le=100, alias="confidenceScore")    scores: ScoreBreakdown    competency_analysis: List[CompetencyAnalysis] = Field(..., alias="competencyAnalysis")    skill_breakdown: List[SkillBreakdown] = Field(..., alias="skillBreakdown")    matching_skills: List[str] = Field(..., alias="matchingSkills")    critical_gaps: List[CriticalGap] = Field(..., alias="criticalGaps")    strengths: List[str]    weaknesses: List[str]    explanation: str    radar_data: List[RadarData] = Field(..., alias="radarData")        class Config:        populate_by_name = Trueclass RecruiterRankingResponse(BaseModel):    """Response model for recruiter candidate ranking"""    total_candidates: int = Field(..., alias="totalCandidates")    ranked_candidates: List[CandidateScore] = Field(..., alias="rankedCandidates")        class Config:        populate_by_name = True# ============================================================================# Resume Parsing Models# ============================================================================class ResumeParseRequest(BaseModel):    """Request model for resume parsing"""    resume_text: str = Field(..., min_length=50, description="Resume text content (minimum 50 characters)")class ResumeParseResponse(BaseModel):    """Response model for resume parsing"""    skills: List[str]    summary: str# ============================================================================# Error Response Models# ============================================================================class ErrorDetail(BaseModel):    """Error detail model"""    message: str    error_type: str = Field(..., alias="errorType")    timestamp: str        class Config:        populate_by_name = Trueclass ErrorResponse(BaseModel):    """Error response model"""    error: ErrorDetail# Made with Bob